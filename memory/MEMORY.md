## Memory Index - RECA Inclusion Laboral (Web)

Leer solo el archivo relevante para la tarea actual.

| Archivo | Cuando leerlo |
|---|---|
| [user_profile.md](user_profile.md) | Primera sesion o si hay dudas sobre el contexto del usuario |
| [architecture.md](architecture.md) | Antes de agregar componentes, rutas o decisiones tecnicas |
| [roadmap.md](roadmap.md) | Al inicio de cada sesion para saber el siguiente frente |
| [forms_catalog.md](forms_catalog.md) | Al trabajar en un formulario especifico |
| [form_production_standard.md](form_production_standard.md) | Antes de migrar o endurecer formularios al estandar productivo |
| [supabase_integration.md](supabase_integration.md) | Al tocar auth, datos o API routes de Supabase |
| [google_integration.md](google_integration.md) | Al tocar Google Sheets o Drive |
| [migration_reference.md](migration_reference.md) | Al contrastar con el repo Tkinter original |
| [notion_workflow.md](notion_workflow.md) | Al documentar en Notion o cerrar fases/QA |
| [transicion_drafts_invisibles_plan.md](transicion_drafts_invisibles_plan.md) | Solo si hace falta recordar invariantes del rollout invisible |

## Estado local breve

- `Finalizacion / hardening F1`, `limpieza estructural F2` y `prewarm hardening F3` ya quedaron aplicados localmente.
- `Prewarm hardening F3` deja estados terminales reales, lease server-side por draft, cleanup remoto best-effort, rollout por `env + pilot`, rate limit y observabilidad mas fina para reuso vs rebuild.
- `Prewarm resiliencia F2` ya quedo aplicada localmente sobre esa base: delete remoto distinguible/idempotente, retencion de cleanup pendiente, backoff cliente por key, validacion de bundle fuera del polling y constraint SQL para `google_prewarm_status`.
- `Prewarm consolidacion estructural F3` ya quedo aplicada localmente: pipeline shared de prewarm/finalizacion en las 8 routes, rollout separado de la registry de dominio, contrato comun de trackers, tipos explicitos en la superficie prewarm/finalizacion y documentacion de la divergencia `legacy_company` vs `draft_prewarm`.
- `Prewarm hardening operativo F3` ya quedo aplicado sobre esa base: rollout `opt-in` real por `env`, registry tipada por formulario, constantes estructurales shared para `Presentacion` y `Sensibilizacion`, firma deterministica de estructura, boundary shared para los 8 `*FormEditor`, helper de gate renombrado a logica pura, tests directos de naming y eliminacion de `serverClient.ts` muerto. Este corte ya tiene preview nuevo y queda pendiente solo QA manual antes de decidir rollout global en prod.
- `Drafts invisibles` ya quedaron armonizados de forma shared para todos los long forms del piloto. La URL nominal ya no debe exponer `?session=draft:<uuid>`.
- `Drafts durante finalizacion` ya quedaron endurecidos de forma shared para bloquear checkpoints automaticos y evitar huerfanos remotos.
- `Observabilidad / Sentry` ya quedo limpiada: el ruido de finalizacion paso a breadcrumbs-only y el backlog reciente del proyecto quedo triageado.
- `Induccion Operativa` e `Induccion Organizacional` ya ejecutan `textReview` real en finalizacion con el mismo patron de los formularios homologados, sin cambiar request hash ni coordinar distinto la idempotencia.
- `Condiciones de la Vacante` ya dejo de observar el formulario completo para el estado de UI; ahora usa un conjunto explicito de campos observados y mantiene el prewarm acotado a `asistentes` y `discapacidades`.
- `Evaluacion` ya recibio un pase conservador de fluidez: la lectura reactiva del formulario quedo separada en grupos logicos (`empresa`, preguntas/accesibilidad y narrativas/asistentes) sin tocar las derivaciones de `section_4` ni `section_5`.
- `Integridad de guardado y salida final F1` ya quedo aplicada localmente: `LongFormShell` fuerza `flush` local al salir de campos editables, el feedback de borrador prioriza el guardado local del dispositivo sin esperar sincronizacion remota, `Evaluacion` mantiene opcionales `section_6` y las observaciones cortas de `2.1` a `3`, y el path shared `draft_prewarm` ahora oculta la hoja provisional vacia para evitar paginas/hojas extra en `Sheet` y `PDF`.
- `Follow-up local de autosave + Evaluacion` ya quedo aplicado sobre esa base shared: todos los long forms siembran una copia local inicial en cuanto el formulario queda listo tras escoger empresa, `flushAutosave()` ya no depende de refs desfasadas para ver cambios pendientes al salir del campo y `Evaluacion` deja de pintar como requeridas las observaciones cortas de `2.1` a `3`. Este follow-up todavia no tiene preview nuevo.
- `Consistencia operativa shared F2` ya quedo aplicada localmente sobre esa base: lookup por cedula mas homogeneo en `Seleccion`, `Contratacion` e inducciones (nombre visible, sin cedula-resumen duplicada y sin alerta duplicada dentro del lookup), gate liviano antes de escoger empresa para los 8 long forms, delete de borradores en `/hub` con desaparicion inmediata sin boton bloqueado persistente y regresion adicional para que `FormCompletionActions` limpie mensajes viejos cuando el siguiente intento si abre recursos. Este corte todavia no tiene preview nuevo.
- `Ergonomia y productividad F3` ya quedo aplicada localmente sobre esa base: `RepeatedPeopleSection` ahora expone el CTA de agregado arriba y abajo para `Seleccion` y `Contratacion`, `AsistentesSection` adopta el patron de lista simple con el CTA solo al final y `Condiciones Vacante` queda cubierto como referencia correcta del patron `solo abajo`. Este corte todavia no tiene preview nuevo.
- `Follow-up local de autosave shared` ya quedo aplicado sobre ese lote: `Seleccion`, `Evaluacion`, `Contratacion`, `Sensibilizacion` y `Condiciones Vacante` dejaron de depender de `isBootstrappingForm` para sembrar la copia local inicial y suscribirse al autosave por cambios. Queda pendiente validarlo en preview porque este fix apunta directamente al riesgo de perdida de datos reportado por QA.
- `Preview integrado de autosave + consistencia shared`: ya disponible en [preview](https://reca-inclusion-laboral-nuevo-hb9xvv292-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/7KyVmyCN1WvcQjQqtSSDtg3RwJfJ). Este corte consolida `follow-up local de autosave + Evaluacion`, `Consistencia operativa shared F2`, `Ergonomia y productividad F3` y el `follow-up local de autosave shared` sobre la misma base actual. Siguiente paso real: QA manual enfocado primero en riesgo de perdida de datos.
- `Preview F3 de prewarm hardening`: ya disponible en [preview](https://reca-inclusion-laboral-nuevo-punvytdw5-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/FAt6ffSYDWW5WeUGeFknLg9trh9f). Este corte consolida el rollout `opt-in`, el endurecimiento de la registry prewarm, la centralizacion estructural de Sheets y el cleanup final de wrappers/tests. Siguiente paso real: QA manual focal de reuse/rebuild, variantes de `Presentacion/Reactivacion`, naming y decision de rollout global.
- `Evaluacion` ya corre como formulario largo en preview, con tarjeta habilitada en `/hub`, publicacion solo a Google Sheets y sin PDF.
- `Delete del hub + apertura guiada acta/PDF` ya quedo aplicado localmente sobre esa base: `useDraftsHub` hace remove optimista con rollback real si el backend falla, `DraftsHub` muestra aviso inline cuando hay rollback y el route DELETE registra timings estructurados por fase (`read_prewarm`, `drive_cleanup`, `db_delete`, `total`). En paralelo, `FormCompletionActions` deja de intentar dos popups en un solo click: el CTA combinado ahora abre primero el acta y guia al usuario para abrir el PDF manualmente desde el CTA separado. Este corte requiere preview nuevo para validar percepcion y UX final.
- `Interprete LSC` ya cerro tambien la `Fase 5` de fixes operativos + readiness final: [preview principal](https://reca-inclusion-laboral-nuevo-33337hnpf-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/6aXYopXRLRxd1GJkzLcN6dcgPsnB) y [preview rehearsal prewarm](https://reca-inclusion-laboral-nuevo-n0131wxp1-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/Fi5Uq7yDgoFVX5Cmk7y4f1CCsJwQ). El flujo real `Sheet + PDF + formatos_finalizados_il` quedo revalidado en preview para casos base y overflow, `payload_raw` ya se persiste, los spreadsheets finales y de prewarm dejan solo `Maestro` visible y el rehearsal con prewarm activado ya responde `ready` usando la plantilla LSC dedicada. El contrato del PDF queda aclarado como privado/protegido: el `pdfLink` se persiste pero requiere login de Google. Decision operativa cerrada: `interprete-lsc` queda listo para activar prewarm por `env`, aunque sigue fuera de `DEFAULT_PREWARM_PILOT_SLUGS` hasta una decision explicita de rollout.
- `Seguimientos` ya quedo implementado localmente hasta Fase 7 sobre un runtime dedicado: entrada por cedula, bootstrap/prewarm por caso, drafts y takeover homogeneos, ficha inicial editable, seguimientos 1..N con override temporal, diff visual contra el ultimo guardado en Google Sheets, helper shared de `visita fallida`, consolidado formula-first con refresh/reparacion de `PONDERADO FINAL` y export PDF selectivo (`solo ficha`, `ficha + seguimiento`, `ficha + seguimiento + consolidado`). Tambien ya quedaron aplicados tres follow-ups locales: `Ficha Inicial Robusta + correccion historica sin autoavance` (`cargo_vinculado` y `discapacidad` dejan de bloquear el caso cuando llegan vacios y un save con `Override` ya no autoavanza), `Politica de PDF persistido` (las variantes PDF ahora salen solo del estado persistido en Google Sheets, el export se bloquea si hay dirty changes locales y el backend endurece mensajes/validaciones contra variantes no listas) y `Cleanup conservador del bundle` (los espejos `direct write` de `Ficha inicial` y `PONDERADO FINAL` ya se sanean como datos reales, el runtime limpia formulas espejo heredadas en bundles existentes y `healthy/stale/broken` deja de auditar esas formulas; las graficas del `PONDERADO FINAL` siguen excluidas y pendientes de rediseño). Sobre esa base ya quedaron aplicadas dos capas de hardening adicionales: `Trust Boundary + guardado seguro` (ownership real por `owner_user_id`, lease server-side de bootstrap por cedula, override firmado y validado en servidor, bloqueo de edicion/export cuando un save responde `written_needs_reload`, validaciones de payload mas estrictas y cobertura automatica ampliada) y `Resiliencia operativa / deuda estructural F2` (hidratacion `eager/passive` para no disparar maintenance writes en cada save, lecturas de followups en paralelo, parseo de drafts mas estricto con workflow reconstruido, preservacion en sesion de overrides validos, helper shared de path access, semantica explicita `keepOnlySheetsVisible`, naming de carpetas mas predecible, logging mas util de temporales PDF y disclaimer honesto de que el check actual del consolidado todavia no audita formulas canonicas). Encima de eso ya quedaron aplicados `Fase 3.1` de QA residual (save coordinado bloqueando `followup_*` con ficha inicial incompleta y bootstrap con errores tipados de storage), el pase de `pulido UX/UI`, `Fase 4` del fix de proteccion/layout, `Fase 5` de ergonomia operativa (atajos masivos de legacy para `Autoevaluacion`, `Evaluacion empresa` y `Evaluacion empresarial` con `Todo excelente/bien/necesita mejorar/mal/no aplica`), la `Fase 1` de continuidad del flujo (`Marcar visita fallida` ahora completa tambien observaciones operativas con `No aplica` dejando intacto `Cierre`, `fecha_fin_contrato` sigue opcional y los submits invalidos de `Ficha inicial` / `Seguimiento` hacen focus al primer error), la `Fase 2` de productividad/consistencia operativa (`Cierre` ya expone dictado en `situacion_encontrada` y `estrategias_ajustes`, `Colapsar` desaparece de `Seguimientos` y la UI ahora deja mas clara la diferencia entre `Guardar borrador`, `Guardar ... en Google Sheets` y `Generar PDF`) y el follow-up estructural actual: las fechas vuelven al selector nativo `type="date"` con estado interno canonico `AAAA-MM-DD`, `Seguimientos` mantiene compatibilidad de lectura con fechas legacy pero ya escribe Google Sheets y PDF en `DD/MM/AAAA`, y `Asistentes` deja de asumir longitud fija `4` para pasar al patron dinamico tipo `Contratacion` con fila `0` de `Profesional RECA`, segunda fila vacia por defecto, CTA `Agregar asistente`, capacidad hasta `10` y expansion del bloque de Sheets a `D47:D56 / N47:N56`. Sobre ese mismo runtime ya quedo aplicado el fix lineal `A+B+C+D`: el parser acepta fechas legacy `D/M/AAAA`, cualquier prefill/hydration interpretable entra al form state ya normalizado a ISO, los valores de fecha no interpretables se sanean a vacio antes de llegar al picker, las routes de guardado ahora devuelven `fieldPath` + `issues` cuando una validacion falla y el cliente usa esa metadata para mostrar el campo exacto y enfocar la etapa activa cuando corresponde. Ademas, `Seguimientos` ya quedo habilitado en el hub y deja de mostrarse como `Próximamente`. Este corte sigue pendiente de preview nuevo y QA manual end-to-end.

- `Seguimientos` ya cerro ademas el fix de override multi-etapa: `override_required` ahora devuelve `missingOverrideStageIds`, el dialogo de `Desbloquear etapa` mantiene spinner y cierre solo al resolver, y la route de override ya acepta varias etapas explicitas en una sola confirmacion para poblar varios grants firmados a la vez.
- `Seguimientos` ya cerro tambien el follow-up de expiracion de override: el TTL del grant sube a `20 min`, el save distingue `override_required` vs `override_expired`, el cliente conserva las etapas faltantes/vencidas como `pendingOverrideRequest` y el mismo dialogo de override ahora sirve para renovar desbloqueos con copy explicito, sin auto-renovacion silenciosa.
- `Seguimientos` ya cerro ademas el follow-up estructural de `Asistentes` y fechas: el followup migra al patron dinamico tipo `Contratacion` con `Profesional RECA` en la fila `0`, segunda fila vacia por defecto, add/remove hasta `10`, Sheets expandido a `10` slots y selector nativo de fecha trabajando en ISO interno mientras Google Sheets y PDF reciben `DD/MM/AAAA`.
- `Seguimientos` ya cerro tambien el hardening post-QA real: los grants de override se persisten por `caseId` en `sessionStorage` y se restauran tras refresh con prune de expirados/invisibles, `visita fallida` deja un aviso persistente en el draft del followup, el backend responde `override_unavailable` (`503`) cuando falta la configuracion del secret, los saves de `Ficha inicial` y `dirty stages` envian `expectedCaseUpdatedAt` para bloquear con `case_conflict` (`409`) cualquier escritura stale, la UI ofrece CTA de recarga explicita para ese conflicto, la CSP agrega `worker-src 'self' blob:` y el server emite logs estructurados de overrides/conflictos. Este corte ya quedo validado localmente con unit tests, lint, build, Playwright y spellcheck; siguiente paso real: preview nuevo y QA manual focal.
- `Preview post-QA de Seguimientos`: ya disponible en [preview](https://reca-inclusion-laboral-nuevo-2nupnjft9-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/GUgyCR3HQFHxDzk37QR9V1qk8EE4). Este corte consolida persistencia de grants por sesion, aviso persistente de `visita fallida`, `override_unavailable`, bloqueo `409 case_conflict`, `worker-src 'self' blob:` y logs estructurados. Siguiente paso real: QA manual focal de refresh, renovacion explicita, conflicto entre pestañas y consola limpia de workers.
- `Seguimientos` ya cerro tambien el fix puntual del token de override: el parser deja de romper tokens con `expiresAt` ISO con milisegundos y ahora separa `expiresAt`/firma usando el ultimo `.` en vez de `split(".", 2)`. El wire format del grant no cambia, los logs distinguen `grant_missing`, `grant_parse_failed` y `grant_signature_invalid`, y el modulo real ya queda cubierto por tests unitarios directos.
- `Preview fix token override`: ya disponible en [preview](https://reca-inclusion-laboral-nuevo-82wd0hpmv-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/6HgTueCAanFC5n26pbZqehjXMT5c). Este corte apunta solo al falso `override_required` causado por tokens validos mal parseados; siguiente paso real: QA manual focal de correcciones historicas y renovacion/expiracion real del grant.
- `Seguimientos` ya cerro ademas el follow-up del `409` propio y la higiene de overrides: el cliente usa `currentDraftData.caseMeta.updatedAt` como unica fuente para `expectedCaseUpdatedAt`, los grants usados se limpian despues de saves exitosos tambien en `sessionStorage`, `Marcar visita fallida` empuja el followup al estado padre en el mismo click para no depender del watch posterior y el editor ya expone `Bloquear etapa` como accion local simetrica al desbloqueo.
- `Seguimientos` ya cerro ademas el follow-up de cambios fantasma y bloqueo explicito de etapa: la autosiembra de `Profesional RECA` en `Seguimiento 2/3` deja de contar como dirty hasta que el usuario toque manualmente la fila `0`, los followups vacios ya no bloquean el PDF solo por abrirse y `Bloquear etapa` ahora pregunta si hay cambios locales para conservarlos o descartarlos antes de volver a proteger la etapa. Este corte ya quedo validado con unit tests, Playwright, build, lint y spellcheck y ya tiene [preview](https://reca-inclusion-laboral-nuevo-ia1p2eqfv-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/8jvkdyKYv9iMSAjBXJkf9NnKqDFX). Siguiente paso real: QA manual focal de `Seguimiento 2/3` vacios, PDF habilitado sin cambios reales y modal de `Bloquear etapa`.
- `Seguimientos` ya cerro ademas el paquete `Fase 1 + Fase 2` de trabajo por hoja: guardar `Ficha inicial` o cualquier `Seguimiento X` ya no autoavanza a la etapa sugerida, el usuario permanece en la misma hoja con mensaje claro de exito y CTAs explicitos a la siguiente etapa visible y a `Resultado final`, y `Desbloquear etapa` / `Bloquear etapa` ahora disparan checkpoint manual del borrador local para dejar la sesion mas consistente al recargar. Tambien se restaura el estado desbloqueado al volver desde draft local y el comportamiento ya queda validado localmente con unit tests, Playwright, build, lint y spellcheck. Este corte no tiene preview nuevo por decision deliberada; siguiente paso real: trabajar `Fase 3 + Fase 4` (variantes PDF siempre visibles + regla de exportabilidad de `visita fallida`).
- `Seguimientos` ya cerro ademas el paquete `Fase 3 + Fase 4` de export PDF: `Resultado final` ahora muestra siempre todas las variantes esperables segun el workflow visible (`3` o `6` seguimientos), cada opcion expone si esta disponible o bloqueada con motivo explicito, el cliente renderiza la lista completa en vez de ocultarla dentro de un select y el backend de export ya valida `enabled/disabledReason` para no permitir variantes visibles pero aun no exportables. Ademas, un seguimiento persistido como `visita fallida` ya cuenta como exportable para `Ficha inicial + Seguimiento X` aunque no cumpla la narrativa estandar completa, mientras que las variantes con `+ Consolidado` siguen exigiendo `summary.exportReady`. Este corte ya quedo validado localmente con unit tests, Playwright, build, lint y spellcheck.
- `Preview Fase 3 + Fase 4 de Seguimientos`: ya disponible en [preview](https://reca-inclusion-laboral-nuevo-crn42nc2c-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/8YtsqmgactXYwaHRVgAk8aBQg4sb). Siguiente paso real: QA manual focal de variantes siempre visibles en casos `no_compensar/compensar`, `visita fallida` exportable, consolidado deshabilitado con motivo y bloqueo global por cambios locales pendientes sin ocultar opciones.
- `Seguimientos` ya cerro ademas el follow-up de feedback visible post-save: los saves exitosos de `Ficha inicial` y `Seguimiento X` ahora disparan un toast local fijo en viewport con mensaje especifico del guardado y CTA a `Resultado final` y al siguiente seguimiento visible, sin reintroducir autoavance. El banner superior `statusNotice` se conserva como estado persistente, pero el panel inline viejo desaparece para que la confirmacion se vea tambien cuando el usuario guarda desde el final del scroll. Este corte ya quedo validado localmente con unit tests, Playwright, build, lint y spellcheck y no tiene preview nuevo por ahora.

- `Interprete LSC` ya cerro tambien la `Fase 3` local de editor web y exposicion: sobre la base de `Fases 1-2` quedaron visibles la tarjeta del hub y el route `app/formularios/[slug]`, se montaron `InterpreteLscForm` y `useInterpreteLscFormState` sobre el runtime shared, se agrego `GET/POST /api/interpretes` con `useInterpretesCatalog` y el flujo completo ya finaliza con `Sheet + PDF + raw payload + formatos_finalizados_il`. La decision `standalone-only` se mantiene, ODS sigue leyendo desde `payload_normalized` y `interprete-lsc` sigue fuera del piloto default de prewarm.
- `Interprete LSC` ya cerro tambien la `Fase 5` con decision binaria opuesta a la de `Fase 4`: el formulario queda listo para activar prewarm por `env`. La unica salvedad vigente no es de funcionalidad sino de sharing: el `pdfLink` es privado y requiere login de Google.
- `Interprete LSC` ya recibio ademas un pase local de `UX/UI y pulido operativo`: gate con copy especifico del servicio, resumen visible con conteos/sumatoria, mensajes disabled mas utiles por seccion, guia rapida de horas/medianoche, combobox de interpretes con empty state explicito y normalizacion de texto libre, y aviso final de que el `pdfLink` requiere login de Google. Este pase ya quedo verificado localmente con `vitest`, `lint`, `build` y `spellcheck`, pero todavia no tiene preview nuevo.
- `Interprete LSC` ya cerro ademas un follow-up puntual del editor: `Oferentes / vinculados` ahora tambien permite lookup por cedula contra `usuarios_reca`, completa `cedula + nombre_oferente`, conserva `proceso` como campo manual y reusa el patron `Cargar/Reemplazar datos` del resto de formularios. Sobre ese mismo ajuste ya quedo corregido un bug de input visual: el card registraba `cedula` dos veces y el lookup no reflejaba lo tecleado en pantalla aunque el draft si detectara cambios. Preview vigente del fix: [preview](https://reca-inclusion-laboral-nuevo-i3avw3o66-auyabans-projects.vercel.app) · [inspector](https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/FcQEYTmjNZi1x56SZty3eZsTDDqi). Este follow-up ya paso `src/lib/usuariosReca.test.ts`, `src/components/forms/shared/UsuarioRecaLookupField.test.tsx`, `src/components/forms/interpreteLsc/InterpreteLscOferentesSection.test.tsx`, `lint`, `build` y `spellcheck`.
- `Interprete LSC` ya cerro ademas un follow-up de payload ODS: `payload_normalized.parsed_raw.interpretes` deja de ser solo lista de nombres y ahora conserva el detalle fila por fila de cada interprete (`nombre`, `hora_inicial`, `hora_final`, `total_tiempo`), mientras `interpretes_nombres` preserva el resumen deduplicado. Esto deja a ODS listo para generar una entrada distinta por interprete desde una sola acta sin depender de `payload_raw`. El ajuste ya paso `src/lib/finalization/interpreteLscPayload.test.ts`, `src/app/api/formularios/interprete-lsc/route.test.ts`, `lint` y `build`.

## Siguiente foco recomendado

- Prioridad inmediata: hacer QA manual corto del preview vigente de `Interprete LSC`, incluyendo el fix visual del lookup por cedula en `Oferentes / vinculados`, horas, Sabana y finalizacion.
- Luego, si se quiere materializar el rollout ya decidido por `env` para `Interprete LSC`, hacer redeploy del deployment productivo para que el cambio en `NEXT_PUBLIC_RECA_PREWARM_PILOT_SLUGS` tome efecto.
- Finalmente, si QA producto necesita auditar el PDF sin login, abrir follow-up especifico de sharing/naming en Drive y retomar QA shared del preview F3 de prewarm hardening para `Presentacion`/`Reactivacion`, `Sensibilizacion` y al menos uno entre `Seleccion` o `Contratacion`.

1. Hacer QA manual corto del preview vigente de `Interprete LSC`, incluyendo el fix visual del lookup por cedula en `Oferentes / vinculados`.
2. Cuando se quiera activar de verdad el prewarm de `Interprete LSC` en produccion, hacer redeploy para que el cambio de `NEXT_PUBLIC_RECA_PREWARM_PILOT_SLUGS` tome efecto.
3. Si QA producto necesita auditar el PDF sin login, abrir follow-up de sharing/naming en Drive.
4. Retomar QA shared del preview F3 de prewarm hardening para `Presentacion`, `Reactivacion`, `Sensibilizacion` y al menos uno entre `Seleccion` o `Contratacion`.
5. Despues de eso, retomar preview especifico de `Seguimientos` y validar casos `compensar` / `no_compensar`, reclaim de ownership, override server-side, recovery `written_needs_reload`, refresh del consolidado y las 3 variantes de PDF.

## Referencias rapidas

- App original (solo lectura): `C:\Users\aaron\Desktop\RECA_INCLUSION_LABORAL`
- Produccion: <https://reca-inclusion-laboral-nuevo.vercel.app>
- Notion canonico: `10 - Estado actual`, `20 - Pendientes priorizados`, `30 - QA y validacion`, `40 - Iniciativas y decisiones`, `50 - Formularios y migracion`
