# Memory Index — RECA Inclusión Laboral (Web)

Leer **solo el archivo relevante** para la tarea actual. No leer todos en cada sesión.

| Archivo | Cuándo leerlo |
|---|---|
| [user_profile.md](user_profile.md) | Primera sesión o si hay dudas sobre el contexto del usuario |
| [architecture.md](architecture.md) | Antes de agregar cualquier componente, ruta o decisión técnica nueva |
| [roadmap.md](roadmap.md) | **Al inicio de cada sesión** — dice qué sigue y en qué fase estamos |
| [forms_catalog.md](forms_catalog.md) | Al trabajar en cualquier formulario específico |
| [form_production_standard.md](form_production_standard.md) | Antes de migrar o endurecer cualquier formulario hacia estándar productivo |
| [supabase_integration.md](supabase_integration.md) | Al trabajar con auth, datos o API routes de Supabase |
| [google_integration.md](google_integration.md) | Al trabajar con Google Sheets o Google Drive |
| [migration_reference.md](migration_reference.md) | Al migrar un formulario desde el proyecto Tkinter original |
| [notion_workflow.md](notion_workflow.md) | Al documentar en Notion, abrir una nueva sesión de trabajo o registrar fases/QA |
| [qa_hardening_summary_2026-04-14.md](qa_hardening_summary_2026-04-14.md) | Al revisar o retomar el cierre del hardening post-QA |

---

## Contexto rápido (leer siempre)

- **Fase actual adicional (2026-04-17):** follow-up local sobre el fix de éxito desde `?draft=` ya aplicado. `Presentación` ahora corta `hydrateRoute()` con `finalizedSuccess` antes de cualquier branch de draft/session, los otros hooks largos alinearon el orden `setFinalizedSuccess(...) -> clearFinalizationUiLock(...)`, y `Condiciones de la Vacante` ya cierra el popup antes de `clearDraftAfterSuccess()`. Revalidación focal cerrada con `npm run lint`, `npm run build` y `npx playwright test e2e/finalization-controlled.spec.ts --workers=1` (`12/12`).
- **Fase actual adicional (2026-04-17):** fix local del success state al finalizar desde `?draft=` ya aplicado en los 7 formularios largos. Cada hook ahora toma un snapshot local `finalizedSuccess` antes de limpiar el borrador, ese modo de éxito gana sobre cualquier hydration/promotion de `draft/session` y la URL `?draft=` se conserva mientras la pantalla de éxito permanece visible. `finalization-controlled.spec.ts` ahora cubre también `Presentación` y `Sensibilización` iniciando desde draft persistido. Validación local cerrada con `npm run test` (`582/582`), `npx playwright test e2e/finalization-controlled.spec.ts --workers=1` (`12/12`), `npm run lint`, `npm run build` y `npm run spellcheck`.

- **Fase actual adicional (2026-04-16):** cierre local del hardening de finalizacion. `processing` ahora usa lock server-side de `6 min` solo como anti-duplicado y el cliente hace polling fijo cada `5s` tras el watchdog de `25s`. Los 4 routes con riesgo en `markFinalizationRequestFailed(...)` ya usan un helper safe compartido, el popup de cierre muestra estados en lenguaje natural y `manualTestFill` ya cubre `Presentacion`, `Sensibilizacion` y `Condiciones de la Vacante`. Playwright ya valida recovery y limpieza de borrador en esos tres slugs. Validacion local final cerrada con `npx vitest run` focal (`62/62`), `npx playwright test e2e/finalization-controlled.spec.ts` (`8/8`), `npm run lint`, `npm run build` y `npm run spellcheck`.
- **Fase actual adicional (2026-04-16):** la alineación canónica del nuevo frente `Inducción Organizacional + Inducción Operativa` ya quedó documentada en `memory/inducciones_f0.md`. El lote se cerró a nivel de alcance con documento largo, `usuarios_reca` low-egress por carga manual, asistentes genéricos, observaciones con dictado y un solo vinculado en web; el siguiente paso real pasa a `F1`: construir la base compartida de inducciones antes de abrir los slices específicos.

- **Fase actual adicional (2026-04-16):** `F1` de ese mismo frente ya quedó implementado localmente y sigue siendo la fundación compartida del lote. El repo ahora tiene `src/lib/inducciones.ts` como contrato shared de la base común, helpers de `usuarios_reca` para vinculado singular de inducciones y los bloques reutilizables `InduccionCompanySection` + `InduccionLinkedPersonSection`.
- **Fase actual adicional (2026-04-16):** `Inducción Organizacional` + `Inducción Operativa` ya quedaron implementadas localmente sobre esa base compartida. Ambos slugs ya cargan como long forms, tienen presenter, hook, validación, routes de finalización y adapters modulares, mientras el `HubMenu` sigue ocultándolos (`available: false`) hasta el siguiente corte de hardening/QA. Validación local del lote cerrada con `vitest` focal (`41/41`), `eslint`, `npm run spellcheck` y `npm run build`. El siguiente paso real pasa a `F4`: hardening técnico conjunto y decisión de exposición en hub después de preview/QA.
- **Fase actual adicional (2026-04-16):** `F4-A` de ese mismo lote ya quedó aplicado localmente. `Inducción Organizacional` + `Inducción Operativa` ahora comparten hashing estable desde `src/lib/finalization/idempotency.ts`, sincronizan un payload mínimo a `usuarios_reca` como paso non-fatal y corrigieron sus exports a Google Sheets contra el maestro vivo: Organizacional ya escribe `medio + recomendacion` explícitos en `section_4` y Operativa corrigió el tramo final de `section_4` sin agregar correos inexistentes en `section_1`. Validación focal cerrada con `vitest` (`43/43`) y `eslint` sobre los archivos tocados; el siguiente paso real pasa a `F4-B` y luego `F5` (preview + QA manual) antes de exponer el lote en el hub.
- **Fase actual adicional (2026-04-16):** `F4-B` de ese mismo lote ya quedó aplicado localmente. `Inducción Organizacional` eliminó validaciones duplicadas de `section_4`, checks muertos del vinculado y endureció el fixture de `section_3`; `Inducción Operativa` simplificó la regla de asistentes sin cambiar el contrato funcional. También se reforzó la cobertura de QA con replay paths en ambas routes, asserts por efectos observables de finalización y un test interactivo del cleanup de snapshot en `InduccionLinkedPersonSection`. Baseline final local cerrado con `npx vitest run` (`551/551`), `npm run lint`, `npm run build` y `npm run spellcheck`; el siguiente paso real pasa a `F5` (preview + QA manual) antes de decidir exposición en hub.
- **Fase actual adicional (2026-04-16):** antes de abrir `F5`, las dos inducciones ya quedaron cubiertas también por Playwright local. El harness E2E ahora incluye `e2e/inducciones.spec.ts` con smoke e integration para `Inducción Organizacional` y `Inducción Operativa` (lookup singular a `usuarios_reca`, recomendaciones derivadas en Organizacional y sync prefijado de `section_4` en Operativa), y `openSeededForm()` ya soporta formularios que no siempre promocionan `?draft/?session` en la URL al primer render. Validación E2E completa cerrada con `npm run test:e2e` (`42/42`); el siguiente paso real sigue siendo `F5` (preview + QA manual).
- **Fase actual adicional (2026-04-16):** `F5` de inducciones sigue en estado `preview` y ya absorbió el segundo follow-up de QA. El deployment vigente pasó a `https://reca-inclusion-laboral-nuevo-m0wqo0f6w-auyabans-projects.vercel.app` con inspector `https://vercel.com/auyabans-projects/reca-inclusion-laboral-nuevo/G3pnfZ4fdV7Z32K3HYPvCeSdgf9a`; además del fix previo de `HubMenu` + `zona_empresa`, el lote ahora deja no obligatorias todas las notas/observaciones de inducciones y ya expone el botón `Test` en `Inducción Organizacional` + `Inducción Operativa` con el mismo patrón de `Selección/Contratación`. Validación local cerrada con `vitest` focal (`23/23`), `eslint`, `npx playwright test e2e/inducciones.spec.ts` (`6/6`) y `npm run build`. El siguiente paso real sigue siendo QA manual focal del preview vigente.
- **Fase actual adicional (2026-04-16):** follow-up local de UX de finalización aplicado sobre todos los formularios largos antes de cerrar `F5`. `Presentación`, `Sensibilización`, `Condiciones de la Vacante`, `Selección`, `Contratación`, `Inducción Organizacional` e `Inducción Operativa` ya mantienen los errores de publicación dentro del popup de guardado con `Cerrar/Reintentar`, y al cerrarlo dejan el mismo feedback contextual junto a `Finalizar` en vez de devolverlo al banner superior. El diálogo compartido (`FormSubmitConfirmDialog`) ya soporta el estado de error y Playwright cubre explícitamente el caso fallido de `Selección` e `Inducción Organizacional`. Validación local cerrada con `npm run lint`, `npm run spellcheck`, `npm run build`, `npx vitest run src/components/forms/shared/FormSubmitConfirmDialog.test.tsx` y `npx playwright test e2e/finalization-controlled.spec.ts e2e/inducciones.spec.ts`; preview vigente actualizado a `https://reca-inclusion-laboral-nuevo-ibf3dbs8t-auyabans-projects.vercel.app`.
- **Fase actual adicional (2026-04-16):** hardening local de confirmación resiliente aplicado sobre los 7 formularios largos. Todas las routes de finalización ya escriben `payload_normalized.metadata.finalization` (`form_slug`, `request_hash`, `idempotency_key`, `identity_key`), existe `POST /api/formularios/finalization-status` como status endpoint autenticado con recovery/backfill desde `form_finalization_requests` y `formatos_finalizados_il`, y el cliente ahora puede pasar de `esperando_respuesta` a `verificando_publicacion` para confirmar una publicación ya persistida sin reenviar el acta. Cuando el publish sigue incierto, el popup conserva el contexto y ofrece `Verificar de nuevo` en lugar de re-publicar a ciegas. Validación local cerrada con `npx vitest run` focal (`50/50`), `npx playwright test e2e/finalization-controlled.spec.ts` (`5/5`), `npm run lint`, `npm run build` y `npm run spellcheck`.

- **Fase actual adicional (2026-04-16):** hardening local del hub ya aplicado. `/hub` ahora resuelve server-side el shell inicial, el nombre del usuario y los borradores remotos, elimina el `fallback` vacío y abre el drawer por `?panel=drafts` desde el HTML inicial sin depender de `useSearchParams()` en el primer render. La reconciliación local sigue cliente-side y corre en background sin volver a tapar la UI. Validación local cerrada con `npm run lint`, `npm run test` (`495/495`), `npm run test:e2e:smoke` (`18/18`) y `npm run build`; preview listo en `reca-inclusion-laboral-nuevo-nrab1go9t-auyabans-projects.vercel.app` con QA focal pendiente.

- **Fase actual adicional (2026-04-16):** hardening local de performance e hidratacion para formularios largos ya aplicado en `/formularios/[slug]`. Las 5 entradas largas ahora cargan por `dynamic()`, los deep links `?draft=...` se resuelven server-side con Supabase SSR + RLS y la precedencia de restore queda `copia local > draft prefetched > fetch cliente fallback/error`. Validacion local cerrada con `npm run lint`, `npm run test` (`489/489`) y `npm run build`; siguiente paso real: preview nuevo y QA focal de carga inicial + restore remoto.

- **Proyecto:** Reescritura web de app Tkinter de gestión de formularios de inclusión laboral
- **Stack:** Next.js 16 + Tailwind v4 + Supabase + Google Sheets/Drive + OpenAI Whisper
- **Restricción crítica:** $0 infra — todo free tier
- **Dev:** Solo developer + Codex como equipo
- **Fase actual:** producción real publicada para piloto con usuarios. `Presentación/Reactivación` sigue siendo la referencia canónica del estándar productivo, `Sensibilización` ya cerró S1-S6 sobre `/formularios/sensibilizacion`, `Contratación` + `Seleccion` ya fueron promovidas a producción y `Inducción Organizacional` + `Inducción Operativa` ya pasaron de `local` a `preview`, con tarjetas habilitadas en `/hub`, notas opcionales, botón `Test` en ambos formularios y errores de finalización retenidos en el popup/contexto de cierre sobre el preview vigente. El siguiente frente recomendado dentro de esta migración pasa por QA manual focal de ese preview y validación de exposición final antes de push/promoción.
- **Fase actual adicional (2026-04-15):** el flujo compartido de `usuarios_reca` ya quedó implementado localmente para `Seleccion` y `Contratacion`, pero sigue pendiente validarlo manualmente dentro del corte de QA antes de push.
- **Fase actual adicional (2026-04-15):** `Seleccion` ya consume `usuarios_reca` con el mismo patron manual de `Contratacion` (`Cargar/Reemplazar datos`, warning por card y highlights por campos modificados), la paridad de sync legacy para dropdowns prefijados de `Seleccion` ya quedó auditada y cubierta con tests, y el repo ya tiene Playwright v1 como smoke local para `hub`, gates, repetibles, lookup por cedula, sync de dropdowns y boton `Test`. Este corte sigue local, sin preview nuevo.
- **Fase actual adicional (2026-04-15):** follow-up local de hardening QA ya cerrado sobre ese mismo corte: `Contratacion` ahora tiene sus reglas de dropdowns prefijados extraidas a `src/lib/contratacionPrefixedDropdowns.ts` con tests directos, el bypass E2E paso a env server-only (`E2E_AUTH_BYPASS`) y las routes read-only de `usuarios-reca` ya lo respetan, `manualTestFill` usa fecha dinamica con cobertura del feature flag y Playwright ya cubre también el flujo de click sobre sugerencia en `Seleccion`. Baseline local verde con `npm run lint`, `npm run test` (`447/447`), `npm run test:e2e` (`11/11`), `npm run build` y `npm run spellcheck`.
- **Fase actual adicional (2026-04-15):** cierre local del frente de performance/ajustes/automation para `Seleccion`. `RepeatedPeopleSection` ya renderiza rows repetibles con watch por fila y helpers de insercion/reindexado dedicados, `Seleccion` centraliza sus 22 statements en `src/lib/seleccionAdjustmentLibrary.ts` con categorias, sugerencias universales y sugerencias por discapacidad, y `SeleccionRecommendationsSection` ahora expone ayudas contextuales sin perder los botones legacy. Playwright ya quedó extendido como capa transversal con smoke de app/formularios, drafts lifecycle, recomendaciones de `Seleccion`, `usuarios_reca`, sync prefijado y finalizacion controlada con mocks. Baseline local verde con `npm run lint`, `npm run spellcheck`, `npm run test` (`458/458`), `npm run test:e2e` (`33/33`) y `npm run build`.
- **Fase actual adicional (2026-04-15):** follow-up local de Fase 1 + 2 sobre `Seleccion` y `Contratacion`. La normalizacion ahora deriva `edad` desde `fecha_nacimiento`, `Seleccion` fuerza `tipo_pension = "No aplica"` cuando `cuenta_pension = "No"`, `Contratacion` deja `fecha_fin` opcional dentro del contrato de validacion y `seleccionAdjustmentLibrary` ya detecta `Autismo` y discapacidades con tildes, mueve `family_boundaries` a `Familia y contexto` y expone una API publica mas corta via `@/lib/seleccion`. También quedaron preparados los tests de regresion de estas reglas, pero por instruccion del lote todavia no se ejecuta el baseline hasta cerrar las fases restantes.
- **Fase actual adicional (2026-04-15):** follow-up local de Fase 4 + 5 del mismo lote pre-prod. `SeleccionRecommendationsSection` se rehizo como flujo guiado de 3 bloques (`Bloques rapidos`, `Sugerencias para esta persona`, `Texto final de ajustes`) sin cambiar el payload de `ajustes_recomendaciones`, y `Seleccion` + `Contratacion` ahora guardan view-state efimero por ruta/sesion para que restore/autosave ya no devuelvan al usuario al tope del formulario. También quedaron preparados los casos de regresion para preview de helpers y conservacion de contexto después de reload, pero por instruccion del lote aun no se ejecuta el baseline hasta cerrar las fases restantes.
- **Fase actual adicional (2026-04-15):** follow-up local de Fase 3 + 6 del mismo lote pre-prod. `Seleccion` y `Contratacion` ya reflejan en UI la edad derivada desde `fecha_nacimiento`; `Seleccion` expone `fecha_firma_contrato` como texto libre y fuerza/deshabilita `tipo_pension` cuando `cuenta_pension = "No"`, mientras `Contratacion` deja `fecha_fin` como texto opcional. Las cards de personas convergieron a una cabecera y bloque inicial mas parecidos, el autofill de cargo del profesional RECA se endurecio por seleccion exacta/blur, y la finalizacion ahora muestra pasos cliente-side + tiempo transcurrido con error contextual cerca de las acciones en vez de depender del banner superior. También quedaron preparados los casos de regresion de esta UX, pero por instruccion del lote aun no se ejecuta el baseline hasta cerrar las fases restantes.
- **Fase actual adicional (2026-04-16):** follow-up local de Fase 7 + 8 del mismo lote pre-prod. `LoginForm` ya corrige ortografia y tildes visibles (`sesión`, `contraseña`, `gestión`, `inclusión`, `Buenas prácticas`), `textReview` amplió la allowlist real de `condiciones_vacante` a campos libres como `nombre_vacante`, horarios, días y observaciones, con sanitización mínima de espacios/saltos de línea sin imponer casing agresivo, y `Seleccion` + `Contratacion` ahora comparten `src/lib/finalization/sheetValueFormat.ts` para exportar `certificado_porcentaje` como número consistente en Google Sheets (`45`, `45%`, `45,5`, `45.5`). También quedaron preparados los tests de regresión de copy, text review y decimal-to-Sheets, pero todavía no se ejecuta baseline hasta cerrar las fases restantes.
- **Fase actual adicional (2026-04-16):** cierre local de Fase 9 + 10 del mismo lote pre-prod. `DraftPersistenceStatus` ya expone `data-save-state` + `data-local-saved-at` para que Playwright espere guardado real sin sleeps, `DraftsHub.tsx` alinea su naming interno con el modulo real, `RepeatedPeopleSection` actualiza `renderRow` via ref + `useSyncExternalStore` sin romper la memoizacion del section, y `Condiciones de la Vacante` movio `Agregar fila` al final del bloque de discapacidades. Durante el hardening también se alinearon los specs de finalizacion controlada y recomendaciones de `Seleccion`, y `ContratacionNarrativeSection` recupero `data-testid` para `ajustes_recomendaciones`. Baseline final local verde con `npm run lint`, `npm run spellcheck`, `npm run test` (`475/475`), `npm run test:e2e` (`36/36`) y `npm run build`. Siguiente paso real: preview nuevo y QA manual final pre-push.
- **Fase actual adicional (2026-04-16):** follow-up local post-preview para `Seleccion` + `Contratacion` sin reejecutar baseline todavia. `RepeatedPeopleSection` ahora permite titulo/subtitulo custom por card, de modo que `Seleccion` y `Contratacion` muestran `Oferente/Vinculado N` como identidad primaria y `nombre + cedula` como resumen secundario cuando existen, eliminando la banda interna duplicada de `Consecutivo`. Además `usuariosReca` ya ignora sentinelas derivados (`grupo_etnico_cual = "No aplica"` sin grupo afirmativo y `tipo_pension = "No aplica"` sin pension afirmativa) al decidir si una fila realmente tiene datos reemplazables, con regresiones nuevas preparadas pero sin correr `lint/test/build` aun por instruccion del lote.
- **Fase actual adicional (2026-04-16):** cleanup tecnico local posterior al mismo QA, también sin rerun de baseline por instruccion. `e2e/setup/auth.setup.ts` ya toma `E2E_AUTH_BYPASS_COOKIE` desde `src/lib/auth/e2eBypass.ts`, las routes de finalizacion de `Seleccion` y `Contratacion` llaman `supabaseClient.auth.getSession()` directamente sin ternario defensivo, y `e2e/finalization-controlled.spec.ts` ya no depende de que el paso visual `esperando_respuesta` permanezca visible cuando la respuesta mockeada llega rapido.
- **Fase actual adicional (2026-04-16):** release ya promovido a `main` y desplegado en produccion. El corte `Seleccion + Contratacion` se publico desde la rama `codex/release-f5-contratacion-seleccion-20260416`, el PR `#5` quedó mergeado con commit `ad5ff0d` y Vercel ya tiene listo el deployment productivo `reca-inclusion-laboral-nuevo-b6bxmxbw2-auyabans-projects.vercel.app` bajo el alias `https://reca-inclusion-laboral-nuevo.vercel.app`.
- **Fase actual adicional (2026-04-15):** fix local urgente sobre finalizacion de formularios: `formatos_finalizados_il` ya vuelve a usar el `usuario_login` canónico del usuario autenticado en lugar de `user.email`, con helper compartido server-side para resolverlo desde `app_metadata` o la tabla `profesionales`. El parche cubre `Presentacion`, `Sensibilizacion`, `Condiciones de la Vacante`, `Seleccion` y `Contratacion`; validacion local focal cerrada con `npx vitest run` sobre 6 archivos (`28/28`) y `npx eslint` sobre las rutas/helpers tocados. Sigue pendiente desplegar y revalidar en producción.
- **Actual local (2026-04-14):** el drawer de borradores ahora deriva labels visuales desde el snapshot local para distinguir mejor drafts del mismo formulario y empresa. `condiciones-vacante` prioriza `nombre_vacante`, `numero_vacantes` y `fecha_visita`; si dos drafts siguen viéndose iguales, el hub agrega badge `Similar x/n`.
- **Estado reportado por usuario:** se autorizó el lanzamiento real a producción para empezar pruebas con usuarios. El deployment productivo quedó publicado desde el workspace actual en Vercel, con alias activo en `reca-inclusion-laboral-nuevo.vercel.app`; el cierre local más reciente del baseline compartido de formularios largos pasó `npm run lint` y `npm run test` (`251/251`) sin crear preview nuevo.
- **Hardening local post-QA:** auth server-side, integración Google, serialización de drafts, limpieza de artefactos huérfanos, unificación de modalidad (`Mixta` canónica con compatibilidad para restore de `Mixto`) y convergencia de formularios largos (`LongFormShell`, `LONG_FORM_SLUGS`, `longFormHydration`, `<slug>Sections`, `<slug>Hydration`) ya quedaron listos localmente y documentados.
- **Actual local adicional (2026-04-14):** `Presentacion` y `Sensibilizacion` ya usan contenedor delgado + hook de estado + presenter puro sobre `useLongFormDraftController`; `npm run lint`, `npm run test` y `npm run build` pasaron localmente.
- **Actual local adicional (2026-04-15):** `Condiciones de la Vacante` ahora también usa contenedor delgado + hook de estado + presenter puro; además quedó endurecido el hash de idempotencia del formulario, `uploadPdf` usa stage consistente sin retry ciego y `textReview` ya tiene timeout + telemetría estructurada (`textReviewStatus`, `reason`, `reviewedCount`, `model`). Validación local cerrada con `npm run spellcheck`, `npm run lint`, `npm run test` (`315/315`) y `npm run build`.
- **Actual local adicional (2026-04-15):** `Seleccion` y `Contratacion` ya tienen `F0` documentado en `memory/seleccion_contratacion_f0.md` y `F1` quedó implementado en la infraestructura compartida de Google Sheets. `FormSheetMutation` ahora soporta `templateBlockInsertions`, `companySpreadsheet` ya reescribe/prepara esas hojas al duplicarlas y el siguiente paso local recomendado es `F2`: cerrar la base compartida de arrays repetibles para oferentes/vinculados antes de migrar `Contratacion`.
- **Actual local adicional (2026-04-15):** `F2` ya quedó implementado. El repo ahora tiene `src/lib/repeatedPeople.ts`, `RepeatedPeopleSection` y helpers genéricos en `validationNavigation.ts` para arrays repetibles; `desarrollo_actividad` se mantiene como campo raíz fuera del array y el siguiente paso local recomendado pasa a `F3`: montar `Contratacion` sobre esta fundación compartida.
- **Actual local adicional (2026-04-15):** `F3` de `Contratacion` quedó implementado localmente con el patrón modular del repo: contenedor + hook + presenter + secciones + schemas/hydration/navigation propios + `POST /api/formularios/contratacion` + adapters de payload/Sheets/PDF. `desarrollo_actividad` quedó como campo raíz único, `vinculados` usa `RepeatedPeopleSection`, la finalización escribe `5. CONTRATACIÓN INCLUYENTE` con bloques repetibles y el cierre local pasó `npm run lint`, `npm run spellcheck`, `npm test` (`366/366`) y `npm run build`. El siguiente paso local recomendado es `Seleccion`.
- **Actual local adicional (2026-04-15):** `F4` de `Seleccion` quedó implementado localmente con el mismo patrón modular del repo: contenedor + hook + presenter + secciones + schemas/hydration/navigation propios + `POST /api/formularios/seleccion` + adapters de payload/Sheets/PDF. `desarrollo_actividad` quedó como campo raíz único, `oferentes` usa `RepeatedPeopleSection`, `section_5` recupera `ajustes_recomendaciones` + `nota` con helpers legacy y la finalización escribe `4. SELECCIÓN INCLUYENTE` con bloques repetibles. El cierre local pasó `npm run lint`, `npm run spellcheck`, `npm test` (`388/388`) y `npm run build`. El siguiente paso local recomendado es abrir QA/push de `Contratacion` y `Seleccion`.
- **Actual local adicional (2026-04-15):** `F5` ya arrancó para `Contratacion` + `Seleccion`. Se creó el preview `reca-inclusion-laboral-nuevo-1yovu360y-auyabans-projects.vercel.app`, el inspector `AyqYxiuE8FbTMj6WPtaf7gxPLj6v`, el branch `codex/f5-qa-contratacion-seleccion` y el baseline técnico volvió a pasar con `npm run spellcheck`, `npm run lint`, `npm run test` (`388/388`) y `npm run build`. Queda pendiente el QA manual de Arquitectura, Dev y funcional sobre ese preview.
- **Actual local adicional (2026-04-15):** se endureció la fase de finalización en `Contratacion` y `Seleccion`: `requestHash` ahora usa `reviewedFormData`, se agregó cobertura para `409 in_progress` + `Retry-After`, y los helpers `buildSection1Data` / `toEmpresaRecord` / runner Google quedaron compartidos en `src/lib/finalization/routeHelpers.ts`.
- **Actual local adicional (2026-04-15):** lote QA local aplicado para `Contratacion` + `Seleccion`: `Contratacion` ya preserva `grupo_etnico_cual="No aplica"` cuando `grupo_etnico="Si"`, `Seleccion` corrige `extra_name` para nombres compuestos, `Contratacion` ahora sí incluye `parsed_raw.extra_name` alineado al legacy, los tests de Sheets usan más constantes del template, y ambos hooks dejaron de usar `useWatch({ control })` global. La verificación contra el repo Tkinter local dejó `no cambiar` para encabezados `VINCULADO N` en los bloques adicionales de `Contratacion`. El baseline local volvió a pasar con `npm run spellcheck`, `npm run lint`, `npm run test` (`395/395`) y `npm run build`. El siguiente paso recomendado es crear un preview nuevo y abrir el QA manual final.
- **Actual local adicional (2026-04-15):** ya existe preview nuevo para ese lote en `reca-inclusion-laboral-nuevo-bj3gi27p2-auyabans-projects.vercel.app` con inspector `EwrP8SvLTXr9paTcuihcbeUtdNDF`. Ese preview pasa a ser el corte correcto para el QA manual final de Arquitectura, Dev y validación funcional antes de commit/push.
- **Actual local adicional (2026-04-15):** follow-up local del mismo frente QA aplicado sin cambiar el comportamiento funcional de `extra_name`: `Seleccion` y `Contratacion` ahora comparten una sola fuente de verdad para `valor significativo` desde `src/lib/repeatedPeople.ts`, `contratacionSchema` ya cubre el regression case `grupo_etnico="Si" + grupo_etnico_cual="No aplica"`, y ambas routes pasan `empresaRecord` a `buildSection1Data` en lugar del payload crudo. La diferencia de `extra_name` entre `Seleccion` y `Contratacion` quedó documentada como paridad intencional con legacy. Además quedó corregido el mock de observabilidad/Sentry en `src/lib/finalization/profiler.test.ts`, por lo que `npm test` completo volvió a pasar (`396/396`).
- **Actual local adicional (2026-04-15):** `usuarios_reca` ya quedó integrado localmente como fuente compartida de personas. `Seleccion` y `Contratacion` sincronizan `usuarios_reca` al finalizar como paso best-effort no bloqueante; `Contratacion` además agrega lookup manual por cédula con `Cargar datos`, precarga campos mapeados desde `usuarios_reca`, resalta en amarillo los campos modificados frente al snapshot cargado y vuelve a sincronizar la fila final al publicar. También quedaron listas las APIs autenticadas `GET /api/usuarios-reca` y `GET /api/usuarios-reca/[cedula]`. Validación local cerrada con `npm run lint`, `npm run test` (`416/416`), `npm run build` y `npm run spellcheck`.
- **Actual local adicional (2026-04-15):** hardening puntual de `usuarios_reca`: `certificado_porcentaje` ya trata `45` y `45%` como equivalentes para comparación visual y prefill de `Contratacion`, `inferUsuarioRecaDiscapacidadCategoria` y `getContratacionUsuariosRecaModifiedFieldIds` quedaron cubiertos con tests directos, `normalizeUsuarioRecaUpsertRow` ahora ignora keys no permitidas, `useUsuariosRecaSearch` aborta fetch en vuelo, el detalle `GET /api/usuarios-reca/[cedula]` pasó a `no-store` y `usuariosRecaServer` ya reutiliza un admin client lazy por módulo.
- **Actual local adicional (2026-04-15):** follow-up de QA manual aplicado sobre `Seleccion` + `Contratacion`. Ambos formularios ahora abren con gate liviano + carga diferida del editor para bajar el costo inicial, el bloque repetible reindexa bien al borrar/agregar personas, `Contratacion` aclara y acelera el lookup por cédula (`Cargar datos` + `Enter`), se portó la sincronización verificada de dropdowns prefijados desde el legacy, se agregó botón local de `Test` para rellenado mínimo en entornos no productivos y las notas dejaron de ser obligatorias. El cierre local volvió a quedar verde con `npm run lint`, `npm run spellcheck`, `npm run test` (`429/429`) y `npm run build`.
- **Actual local adicional (2026-04-15):** cierre local del siguiente lote sobre `Seleccion` + `Contratacion`: `Seleccion` ahora también consume `usuarios_reca` con lookup manual por cedula y el mismo patron visual de snapshot/highlight que `Contratacion`; las reglas de sincronizacion legacy de dropdowns prefijados de `Seleccion` quedaron extraidas a contrato testeable (`src/lib/seleccionPrefixedDropdowns.ts`) y auditadas contra el legacy; y se agrego Playwright v1 como smoke E2E local (`@playwright/test`, `playwright.config.ts`, specs en `e2e/`) con bypass de auth solo para desarrollo/test. El baseline local quedó verde con `npm run test:e2e` (`10/10`), `npm run lint`, `npm run test` (`439/439`), `npm run build` y `npm run spellcheck`.
- **App original (NO tocar):** `C:\Users\aaron\Desktop\RECA_INCLUSION_LABORAL`
- **Dev server:** `npm run dev` → http://localhost:3000
- **Producción:** https://reca-inclusion-laboral-nuevo.vercel.app
- **Notion canónico:** `00 — Start Here`, `10 — Estado actual`, `20 — Pendientes priorizados`, `30 — QA y validación`, `40 — Iniciativas y decisiones`, `50 — Formularios y migración`, `60 — Sesiones de trabajo`, `70 — Legacy y referencias`, `80 — Operación GitHub`
- **Lectura Notion por defecto:** empezar por `10` y `20`; abrir `30`, `40` o `50` solo si la tarea lo necesita; no leer `60` o `70` salvo bloqueo real

## Lo que ya está construido

### Infraestructura
- ✅ Setup completo Next.js 16 + Tailwind v4 + shadcn/ui + dependencias
- ✅ Paleta de colores RECA (`#81398A`) en globals.css
- ✅ Deploy en Vercel conectado a GitHub (auto-deploy en push a `main`)
- ✅ MCP de Vercel y Supabase configurados en Codex

### Auth
- ✅ Auth con Supabase — login por `usuario_login` (no email directo)
- ✅ `src/proxy.ts` (Next.js 16) — protege `/hub` y `/formularios/*`
- ✅ `src/hooks/useAuth.ts` — expone `user`, `session`, `loading`, `signOut()`
- ✅ Login UI (`/`) — lookup de `usuario_login` en tabla `profesionales`, luego `signInWithPassword()`
- ✅ Hardening mínimo de auth/API — `lookup` con validación Zod, throttling best-effort por IP, errores genéricos y límites de longitud en login
- ✅ Hub / menú con 9 tarjetas (`/hub`) — auth guard activo
- ✅ Usuario de prueba: `aaron_vercel` / `Password1234`

### Búsqueda de empresa
- ✅ `Section1Form` con búsqueda debounce (300ms) en tabla `empresas` (1134 registros)
- ✅ Zustand store (`src/lib/store/empresaStore.ts`) — empresa persiste en `sessionStorage`
- ✅ Ruta dinámica `/formularios/[slug]` → editor canónico para `presentacion` y `sensibilizacion`; `Section1Form` para los demás slugs
- ✅ `/formularios/[slug]/seccion-2` → ruta legacy; en `sensibilizacion` redirige a la canónica

### Formulario Presentación/Reactivación (`presentacion`) — base funcional completa, en hardening para MVP piloto ✅
- ✅ Documento largo en una sola página sobre `/formularios/presentacion`
- ✅ Secciones visibles: empresa, datos de la visita, motivación, acuerdos y observaciones, asistentes
- ✅ Búsqueda y selección de empresa integrada dentro del mismo documento
- ✅ Navegación lateral por secciones en desktop + navegación compacta en móvil
- ✅ Flujo Google Sheets: copia template → escribe celdas → checkboxes → PDF → Drive
- ✅ Guarda en `formatos_finalizados_il` en Supabase
- ✅ Pantalla de éxito con links al Sheet y PDF

### Formulario Sensibilización (`sensibilizacion`) — baseline productivo cerrado ✅
  - ✅ Documento largo funcional sobre `/formularios/sensibilizacion`
  - ✅ Shell reutilizable compartido con `Presentación/Reactivación`: navegación lateral, tarjetas colapsables y `DraftPersistenceStatus`
  - ✅ Superficie simplificada: se retiraron `Temas` y `Registro fotográfico` del formulario web
  - ✅ Finalización solo genera Google Sheet; este formulario ya no exporta PDF
  - ✅ Compatibilidad de borradores preservada mediante mapping `step -> section`
  - ✅ Ruta legacy `/formularios/sensibilizacion/seccion-2` redirige preservando `draft`, `session` y `new`
  - ✅ API `POST /api/formularios/sensibilizacion`
  - ✅ Flujo Google Sheets: hoja `8. SENSIBILIZACIÓN` → observaciones + asistentes
  - ✅ Guarda en `formatos_finalizados_il` en Supabase
  - ✅ S3 técnico cerrado: asistentes significativos, `cargo` obligatorio por fila usada, saneamiento antes de finalización y restore/checkpoint con precedencia explícita
  - ✅ S4 técnico cerrado: política explícita de asistentes por formulario, helpers compartidos por modo y cobertura automática del shell largo
  - ✅ S5 cerrado: QA funcional + QA de regresión aprobadas
  - ✅ S6 cerrado: documentación local/Notion actualizada y promoción del playbook como base para `Inducción Operativa`

### Features reutilizables (disponibles para todos los formularios)
- ✅ `useFormDraft` hook — autosave local-first por sesión/acta + checkpoints remotos low-egress con promoción lazy `session -> draft` + bloqueo multi-pestaña
- ✅ Ciclo de drafts endurecido: alias `session -> draft`, proyección única para hub+contador, timeout visible de guardado manual, purga completa local/remota al finalizar o borrar y retorno al hub sin depender de `window.opener`
- ✅ QA manual ya validó en `Presentación`: warning antes de recargar/cerrar, recuperación del mismo draft tras reload, ausencia de duplicados en el caso probado de cierre con operación colgada, contador consistente y limpieza correcta después de finalizar con éxito
- ✅ Ajuste aplicado en formularios: el submit inválido ya no hace `reset(...)` ni espera el guardado del borrador dentro de `handleSubmit`; el checkpoint ocurre en background para evitar que `Finalizar` quede colgado cuando falla validación
- ✅ Hub canónico: al abrir un borrador desde el drawer ahora se limpia `?panel=drafts`, por lo que refrescar el hub debe volver a `/hub`
- ✅ CSP de preview: `https://vercel.live` se habilita solo en previews de Vercel para eliminar el warning del script de feedback sin relajar producción
- ✅ QA manual cerrada en `Presentación` para el caso crítico del asesor: blur vacío sin crash, bloqueo correcto al finalizar y sin duplicados
- ✅ `AsistentesSection` — modo explícito por formulario: `Profesional RECA + Asesor Agencia` o `Profesional RECA + asistentes libres`
- ✅ `ProfesionalCombobox` — busca en tabla `profesionales`, auto-llena cargo
- ✅ `useProfesionalesCatalog` + `useAsesoresCatalog` — caché por pestaña (TTL 5 min) para catálogos reutilizados
- ✅ `DictationButton` — dictado con OpenAI `gpt-4o-mini-transcribe` via edge function `dictate-transcribe`
- ✅ `LongFormShell` — shell compartido para formularios largos con navegación lateral, tarjetas colapsables y estado de borrador
- ✅ Módulos puros reutilizables para formularios largos — `src/lib/longFormHydration.ts`, `src/lib/presentacionHydration.ts`, `src/lib/sensibilizacionHydration.ts`, `src/lib/presentacionSections.ts` y `src/lib/sensibilizacionSections.ts`
- ✅ Formularios largos: navegación lateral desktop que acompaña el documento
- ✅ Formularios largos: textareas extensos autoexpandibles sin scroll interno
- ✅ `FormField` — wrapper label + input + error + hint
- ✅ Hub persistente en `/hub` con drawer de borradores, deep link `?panel=drafts` y apertura de actas en nuevas pestañas
- ✅ Hub de borradores unificado (local + Supabase) con contador low-egress basado en metadata
- ✅ Apertura segura de borradores ya bloqueados con modal previo y reutilización de locks cliente-only

### APIs disponibles
- ✅ `POST /api/formularios/presentacion` — flujo completo Sheets + PDF + Supabase
- ✅ `POST /api/formularios/sensibilizacion` — flujo Sheets + Supabase, sin PDF
- ✅ `GET /api/asesores` — catálogo de asesores desde tabla `asesores` con cache headers privados
- ✅ `GET /api/profesionales` — lista de profesionales RECA desde tabla `profesionales` con cache headers privados
- ✅ `POST /api/auth/lookup` — lookup de `usuario_login` para auth


