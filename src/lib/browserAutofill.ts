export const BROWSER_AUTOFILL_OFF_PROPS = {
  autoComplete: "off",
} as const;

export const BROWSER_AUTOFILL_SEARCH_GUARD_PROPS = {
  autoComplete: "off",
  autoCorrect: "off",
  autoCapitalize: "none",
  spellCheck: false,
} as const;
